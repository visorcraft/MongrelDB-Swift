Swift toolchain not available in this environment (command -v swift / swiftc empty).
Implemented MongrelDB 0.64 client features offline; unit tests were not executed.

Features:
- CommitHlc / DurableOutcome / QueryStatus (last_commit_hlc, serialization_state, nested durable)
- MongrelDBClient.queryStatus / cancelQuery
- MongrelDBClient.retrieveText → POST /kit/retrieve_text
- DurableRetrieveTests (structural HLC, retrieve_text wire shape, multi-retriever SearchBuilder)
- MongrelDBVersion.string = 0.64.0

To run tests when Swift is available:
  cd /work/repos/visorcraft/mongreldb_swift && swift test --filter DurableRetrieveTests
